import XCTest
@testable import ReusableAuthentication

final class ReusableAuthenticationTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReusableAuthentication().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
